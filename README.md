
  # Create Microphone Customizer UI (Copy)

  This is a code bundle for Create Microphone Customizer UI (Copy). The original project is available at https://www.figma.com/design/v5QoLsoWqeevKnTgmc5f1N/Create-Microphone-Customizer-UI--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  